# Behaviour changes on the published API

> Every change that alters what an exported `serverpackcreator-api` call *does*, without necessarily
> changing what it *looks like*. Moved out of the root `CLAUDE.md` so it stops loading into every session:
> it is reference material for writing release notes and for answering "will this break an embedder?",
> not context a session needs up front. The **policy** it serves stays in the root `CLAUDE.md` under
> *API compatibility policy* — read that first; this is its evidence.
>
> **Source-compatible is not the same as behaviour-compatible.** A change that keeps every signature but
> alters what an exported call returns is still a contract change, and belongs in the release notes even
> though nothing fails to compile. Append a row here when you make one.

| Change | Effect on an embedder |
|---|---|
| `PathsConfig.homeDirectory` consults `-Dde.griefed.serverpackcreator.home` **before** the stored preference and the properties file (`PathsConfig.kt:106`) | A host that sets that property now resolves a different home than the same code did before. Additive and opt-in — nothing changes unless the property is set — but every plugin reading `apiProperties.homeDirectory` follows it. |
| `ApiProperties.resolvePreferencesNode` + `PREFERENCES_NODE_PROPERTY` / `PREFERENCES_NODE_ENV` / `DEFAULT_PREFERENCES_NODE` | New exported surface; the default node name is unchanged, so existing installations keep reading their own settings. |
| The eight `default*ScriptTemplate` properties are computed per access (`PathsConfig.kt:586`–`:643`) instead of captured at construction, and the `ApiProperties` facades (`:801`–`:836`) pass that through | For a stable home the value is identical, so nothing changes for a normal embedder. What changes is that the value is no longer a *constant*: a host that moves the home at runtime (`--home`, the `-D` override, the GUI settings panel) now sees the template paths follow it, where before they kept pointing into the old home. Anything caching one of these paths across a home change was reading a stale path and should re-read instead. |
| `MinecraftServer` gains defaulted `downloadCooldown` / `clock` parameters, and a failed manifest **download** is not re-attempted for an hour (`MinecraftServer.kt`, `readServerJson`) | A manifest already on disk is still always read, so a working installation is unchanged. What changes is a *failing* one: an embedder that previously saw a download attempt — and `WebUtilities`' ERROR-with-stack-trace — on every `getServer`/`requiredJavaVersion` call now sees at most one per hour per version. The exported `Optional` shape is unchanged; a caller reading empty as "no server available" still gets that. |
| `ServerPackProvisioner.variables` reads the shipped `server_files/variables.txt` instead of a compiled-in string literal, falling back to the bundled copy (`ServerPackProvisioner.kt:56-63`). New exported members: `PathsConfig.defaultVariablesTemplate`, `ApiProperties.defaultVariablesTemplate` | A default installation gets byte-identical output — but the value is no longer a constant. An operator who edits that file changes what **every** embedder's generation emits, and one who deletes it gets the bundled fallback (the app's delete-watcher restores it). Anything asserting on a fixed `variables` string should read the template instead. |
| `ServerPackHandler.modFileEndings` and `ConfigurationHandler.zipCheck` become getters reading `ModListCompiler.modFileEndings` / `ModpackZipInspector.zipCheck`, which are promoted from `private` to public (new exported surface) | Both facades return the identical value they always did, so nothing observable changes today — this is listed because they are no longer *constants*: each is now one object shared with its owner, where before the facade held a separate equal-valued copy. An embedder comparing either by identity (`===`) against the owner's now succeeds where it previously failed; one mutating a captured reference would affect both, though both values are immutable. |
| `modscanning` gains `MissingDescriptorException`, and `DescriptorScanner.read` becomes public | Additive. The exception extends `IOException`, which the descriptor readers already declared, so an existing `catch (IOException)` is unaffected — what changes is that an absent descriptor is now *distinguishable* from a failed read, which is what lets the scanners log it at DEBUG instead of ERROR. An embedder calling `read` directly can act on that distinction; `scan` still flattens both to a default entry. **`ScanningException` is removed** — it was `internal`, so nothing outside `-api` could reference it. |
| `modscanning` gains `ModJarScanner`, `DescriptorScanner`, `JsonDescriptorScanner`, `FabricFamilyScanner`, `QuiltPackScanner` and `ModScanner.scannerFor` / `ModScanner.quiltPackScanner`; the `internal` `Scanner<T, U>` and the published `JsonBasedScanner` are **removed** | Mostly additive: a plugin can implement a scanner for the first time, and `Scanner<T, U>` was `internal` so nothing outside `-api` could ever reference it. Every concrete scanner keeps its class name, its public members and its `scan(Collection<File>): List<ScannedMod>` signature. **The one break:** `JsonBasedScanner` is gone rather than deprecated — a subclass compiled against it will not compile, and must extend `JsonDescriptorScanner` instead (same `getJarJson`, plus the scanning contract). Griefed's explicit call on 2026-08-15 overriding the policy below, on the grounds that scanners are not a pf4j extension point: a plugin could subclass the helper but never register the result, so the facade was cost without reachable benefit. |
| `ListUtilities.parallelMap` defaults its `context` to `Dispatchers.Default` instead of `newSingleThreadContext("parallelMap")` (`ListUtilities.kt:213`) | **Behaviour change on published API, and the second half of it is not just a repair.** The leak half is unambiguous: the old default handed out a dispatcher owning a dedicated thread that its creator must `close()`, which a defaulted parameter can never do, so every call stranded one thread for the life of the JVM (measured: 4 calls → 4 surviving threads named `parallelMap`). The half to actually read before upgrading: elements now run on the shared processor-sized pool rather than being confined to one thread, so an embedder whose lambda mutated shared state **without synchronisation was previously serialised by accident and can now race**. Signature unchanged, so a caller passing its own context sees nothing. Zero call sites inside this repo — the exposure is entirely embedders and plugins. Pinned by `ListUtilitiesTest.parallelMapDoesNotLeakAThreadPerInvocation` / `…RunsElementsOnMoreThanOneThread`. |
| Building `-api` against kotlinx-coroutines **1.11.0** raises the *runtime* floor to coroutines ≥ 1.11.0 | **Not a source change at all — nothing fails to compile, and that is exactly why it belongs here.** 1.11.0 renames the Kotlin-facing `runBlocking` to JVM name `runBlockingK` (verified with `javap`: `BuildersKt.runBlockingK` exists in 1.11.0, is **absent** in 1.10.2; our compiled `VersionMeta.class` emits `invokestatic BuildersKt.runBlockingK`). An embedder whose resolution **pins** coroutines to 1.10.x — a strict constraint or a BOM — gets `NoSuchMethodError` at runtime, not a build failure. Normal resolution upgrades and hides this. The break is one-directional: old bytecode calling the old name still links against 1.11.0. Widened by `parallelMap` being `inline`, which bakes the call into every downstream caller's own bytecode. **This is not hypothetical — it hit our own `-app` first** (see the build-layout landmine below), so assume it will hit any embedder on a Spring Boot BOM. |
| `WebserviceConfig.DATABASE_URI_KEY` changes **value** from `spring.data.mongodb.uri` to `spring.mongodb.uri`; new `WebserviceConfig.LEGACY_DATABASE_URI_KEY` holds the old name | **No signature changed, and that is exactly why it belongs here — the *content* of an exported constant is the contract.** Spring Boot 4.0.0 retired the old key (metadata `deprecation.level = "error"`, replacement `spring.mongodb.uri`), so writing it bound nothing and Boot silently used its own default `mongodb://localhost/test` — measured, `hosts=[localhost:27017]` and `credential=null` however the URI was configured. An embedder that reads `DATABASE_URI_KEY` to locate the setting is unaffected and in fact fixed. One that **hard-coded** the string `"spring.data.mongodb.uri"` now writes a key nothing reads, and should switch to the constant or to `LEGACY_DATABASE_URI_KEY`. Reading is backward-compatible: `databaseUri` falls back to the legacy key when the live one is absent and re-writes the value under the live one, so an existing properties file needs no edit. The legacy line is deliberately **left in place** rather than deleted, so downgrading to a pre-Boot-4 ServerPackCreator still finds its URI. Also tightened in the same accessor: the scheme check enumerates `mongodb://` / `mongodb+srv://` instead of `startsWith("mongodb")`, so the degenerate `mongodb:` a partly-configured container produced is now rejected at the property rather than deep in the driver. Pinned by `DatabaseUriPropertyTest` (including a guard that fails on any key Boot has retired) and `WebserviceConfigTest`. |
| `WebserviceConfig.FALLBACK_DATABASE_URI` loses its backslashes: `mongodb://user:password@localhost:27017/serverpackcreatordb` instead of `mongodb\://user\:password@localhost\:27017/…` | **The old value was not a URI.** Escaping colons belongs to the `.properties` file format — `Properties.store` applies it on write, `Properties.load` reverses it on read — so a backslash in the *value* was a literal backslash, and `com.mongodb.ConnectionString` accepts only `mongodb://` / `mongodb+srv://`. Every fresh web installation therefore started from a URI the driver rejects, and a generated home read `spring.mongodb.uri=mongodb\\\://…` — three backslashes for one colon, escaped twice. An embedder comparing `FALLBACK_DATABASE_URI` against a hard-coded backslashed copy will stop matching; one passing it to the driver now gets a value that works. Self-healing for existing installs: a stored value with literal backslashes fails the scheme check and is replaced by this one, then written back correctly. Pinned by `WebserviceConfigTest.theFallbackIsItselfAUsableUri`, which also asserts the *absence* of backslashes so the escaping cannot be re-added as a "fix". |
| `WebserviceConfig` gains `hasLegacyDatabaseUri` | Additive, read-only, and narrow on purpose: it reports whether a URI is still stored under the retired pre-Boot-4 key. Unlike `databaseUri` it touches nothing, which is the point — it exists so the 9.0.0 migration can tell the operators the rename affects from the ones it does not, without the read itself normalising the store and thereby destroying the very signal it is testing for. |
| `ModListCompiler` / `MetadataScanner` pick Forge's scanner by comparing the whole Minecraft version instead of its minor component | **Behaviour change, and the point of the fix.** An embedder generating a pack for Forge on a `YY.x.y` Minecraft (26.x) previously got no clientside detection at all — every jar failed the annotation scan and was kept — and now gets the `mods.toml` scan that actually works. A pack that relied on "nothing is ever auto-excluded" will start excluding mods; that is the bug being fixed, not a regression. A version that cannot be parsed at all no longer throws out of `compileModList`, it falls back to the modern scanner. |
| Every outbound HTTP call now carries a connect- and read-timeout, configurable via the new `NetworkConfig` group (`WebUtilities.openTimedConnection` / `openTimedStream`; new exported `ApiProperties.networkConnectTimeout` / `networkReadTimeout` / `networkDownloadReadTimeout`) | **Behaviour change on published API, and the one most likely to be *wanted*.** Nothing set a timeout before, so `isReachable`, `downloadFile`, `getResponseAsString`, `getResponseCode`, `createHasteBinFromString` and the manifest refresh all inherited the JDK's "wait forever". An embedder on a very slow link, or one whose host black-holes packets, now sees a `SocketTimeoutException`-driven failure where it previously blocked indefinitely — which is why the values are properties, not constants: raising them restores patience, and **`0` is honoured as the JDK's no-timeout**, i.e. the exact old behaviour. Defaults 5 s connect / 15 s read / 60 s download-read. Note read-timeout bounds a *single* read, not the whole transfer, so the download value is not a transfer budget. Pinned by `WebUtilitiesTimeoutTest` against a loopback server that accepts and never answers. |
| `UpdateConfig.updateFallback` and (in `-app`) `VersionChecker.getResponse` are bounded by the same `NetworkConfig` timeouts, via the new exported `URL.timedConnection(connectTimeout, readTimeout)`; `NetworkConfig` gains `DEFAULT_CONNECT_TIMEOUT` / `DEFAULT_READ_TIMEOUT` / `DEFAULT_DOWNLOAD_READ_TIMEOUT` | **Behaviour change on published API, and the completion of the timeout work rather than an extension of it.** An audit found the "every outbound call is bounded" claim false: these two were still unbounded, and `updateFallback` is the worst-placed of all — `ApiProperties`' own `init` calls it, so an embedder constructing `ApiProperties` against a silent host blocked *there*, before `stageOne` returned and before the manifest checks that were bounded first. Both now fail with a `SocketTimeoutException`-driven error where they previously waited forever; both are configurable through the same properties, and `0` still restores the old unbounded behaviour. `URL.timedConnection` is exported because a settings group cannot use `WebUtilities` (it is constructed *from* `ApiProperties`, so the dependency would close a cycle) and needed the mechanism without a second copy of it. Pinned by `UpdateConfigTimeoutTest` and `VersionCheckerTimeoutTest`. |
| `ModpackManifestParser.manifestCandidates(destination)` and the `ConfigurationHandler.manifestCandidates` facade | Additive: the six launcher-manifest paths `checkManifests` consults, existing or not, most-specific first. Exported so a caller can ask whether a modpack's manifests *changed* without duplicating the paths — the GUI's config-editor keys a memo on their size and mtime to avoid re-parsing a multi-megabyte `minecraftinstance.json` on every keystroke-pause. Nothing existing changes: `checkManifests` consumes the same list it now returns, and the order follows the branches it always took. The list deliberately includes files that do **not** exist, because a memo has to notice a manifest about to be created. Pinned by `ManifestCandidatesTest`, including that the facade reads the parser rather than re-declaring the paths. |
| `ModListCompiler` builds one `FilterMatcher` per generation: the exclusion-filter setting is read once and REGEX/EITHER patterns are compiled once per entry, and a malformed entry is logged-and-skipped instead of thrown | **Behaviour change, and the point of it.** Under `REGEX`/`EITHER` a single malformed clientside-list entry used to throw `PatternSyntaxException` straight out of `compileModList`, aborting the whole generation; now that entry never matches, its error is logged once per generation rather than once per mod, and every other entry still applies. An embedder relying on the throw to detect a bad list must read the log instead. The performance half is real but small — measured at 300 mods x 550 entries, the per-comparison property read was ~3 ms and the per-comparison `Pattern.compile` ~20 ms, so do not expect a visible speed-up. Pinned by `ModListCompilerHotLoopTest`. |
| `ModpackZipInspector` reads a modpack archive's central directory **once** per inspection, and gains a defaulted `openZip` constructor parameter | Additive and behaviour-preserving in what it returns; the change is in how much I/O it costs. `checkZipArchive` shared one open between its validity check and its base-directory scan (was two), and `getAllFilesAndDirectoriesInModpackZip` partitions one header pass (was two opens via the per-kind methods). Measured 79.9 ms per central-directory read on a 10,000-entry archive, so ~80 ms saved at each site, scaling with the archive. `getDirectoriesInModpackZipBaseDirectory` keeps its signature and delegates to the new private `baseDirectoriesOf`. The constructor parameter is defaulted, so Kotlin still emits a no-arg constructor and Java callers are unaffected; it exists because open-counts are otherwise unobservable (`ModpackZipInspectorOpenCountTest`). |
| `GenerationConfig.clientsideModsRegex` / `modsWhitelistRegex` (and their `ApiProperties` facades) return a fresh `TreeSet` per read instead of one shared, cleared-and-refilled field | **Behaviour change on published API.** Same values, same signatures — but the caller no longer receives an alias of the config's own state. Before, a held result was emptied and rewritten on the next read, and because clear-then-refill is not atomic a concurrent reader could observe the set *part-way through* (briefly wrong, not merely stale) — reachable, since the GUI reads settings from a `parallelStream` walk over its open tabs. Both properties change from `var … private set` to a computed `val`; verified dead first, the private setter was never assigned in `-api` or `-app`. Pinned by `GenerationConfigTest.regexVariantListsAreNotSharedBetweenReads`. |
| `ForgeAnnotationScanner.dependencyCheck` / `dependencyReplace` become `val`s instead of `get() = "…".toRegex()` | Source-compatible, and the same *shape* of change this table already records for `modFileEndings` and `zipCheck`: the value is no longer created fresh on every read, so all callers now share one `Regex`. Nothing observable changes for a caller that matches with it — `Regex` is immutable and matching allocates its own matcher — but one comparing by identity (`===`) across two reads now succeeds where it used to fail. The point of the change is that the getter recompiled the pattern on every read, inside per-dependency loops. A private `additionalDependencyRegex` holding the *identical* literal is gone with it, so the pattern now exists once; it was what the two `additionalDependency*` checks actually used, meaning an edit to the documented copy would have changed nothing there. Minecraft 1.12-and-older path only. |
| `ConfigurationHandler.getAllFilesAndDirectoriesInModpackZip` loses partial results when the archive cannot be read | **Behaviour change on an error path, and the one thing about the single-pass rewrite that is not a pure win.** It used to fetch directories and files in two separate calls, each with its own `try`/`catch`, so a failure in one still returned the other's results and logged two errors. It is now one pass over the headers, so a failure returns an empty list and logs once. Barely reachable — both calls opened the *same* archive, so a failure in one would almost certainly have failed the other — but an embedder that treated a partial list as usable now gets nothing instead. |
| `WebUtilities.openTimedConnection` returns `URLConnection`, not `HttpURLConnection` | Additive, but the choice is load-bearing and must not be "tightened". The timeout setters live on `URLConnection`, so narrowing gains nothing — and `downloadFile` is published API accepting any `URL`: a `file:` URL yields a `FileURLConnection`, and casting that throws `ClassCastException`, which is **not** an `IOException` and so escapes every caller's error handling. Callers needing `responseCode` cast for themselves, as they always did. Pinned by `WebUtilitiesTimeoutTest.aNonHttpUrlCanStillBeDownloaded`. |
| `VersionMeta`'s manifest refresh drops its `isReachable` pre-check and sends `If-Modified-Since`; the logic now lives in the new **internal** `versionmeta.ManifestUpdater` | **Fewer requests and less data, with one visible log change.** A check cost two full requests (a reachability GET whose body was discarded, then the real fetch — and since the probe `disconnect()`ed without draining, the real request paid a fresh TCP+TLS handshake); it now costs one, so startup went 24 → 12 requests. An unchanged manifest that answers `304` is no longer downloaded or parsed at all: measured, 275,153 of 489,038 bytes per startup, and ~601 ms → ~392 ms median batch wall-clock. A host ignoring the header answers `200` and the version-count comparison gates the replacement exactly as before, so no per-host special-casing exists. **What an embedder watching logs will notice:** an unreachable host still logs one WARN per manifest (deliberately preserved — the failure moved onto the `IOException` path, which would otherwise have printed twelve ERRORs with stack traces on every networkless launch), with the exception itself demoted to DEBUG; the absent-manifest CRITICAL message now names the download failure instead of "unreachable". |
| `VersionMeta` no longer refreshes its manifests during construction; it refreshes on a background job and gains `awaitManifestRefresh(timeoutMillis)`. `ConfigurationHandler.checkConfiguration` awaits it | **Behaviour change on published API, and the point of the change.** Construction used to block until all twelve manifests had been checked, so the metas held upstream-fresh data the moment the constructor returned. They now hold the jar-seeded data immediately — `ApiWrapper.setup()` has already written every manifest — and the refreshed data shortly after. Measured with the same probe on the same machine: **~399 ms median before (399/390/835), ~47 ms after (56/36/47)**, and far more when offline, where the old path waited out twelve connect timeouts. An embedder that read a meta immediately after construction and assumed upstream-fresh data now gets seeded data unless it calls `awaitManifestRefresh` — which is idempotent, effectively free once the refresh has landed, and bounded so an unreachable host delays rather than hangs. `checkConfiguration` awaits on the caller's behalf, so version *validation* is unaffected: that is the one choke point every CLI, interactive and web path passes through. The GUI awaits before building its version dropdowns, which are built once and never repopulated. `update()` is unchanged and still blocking. Concurrent re-parse while another thread reads a meta is not new — the web backend's `VersionRefreshSchedule` has always called `update()` on a cron while requests read them. |
| `ApiProperties`' constructor throws `IllegalStateException` when the resolved home directory is not a writable directory, and the `logLevel` setter no longer propagates an `IOException` | **Behaviour change on both ends of the same defect.** Before, an unusable home was *not* checked: `init` wrote `log4j2.xml` inside a `try`/`catch` that only printed, then read that same file unguarded, so construction died with `java.io.FileNotFoundException: /log4j2.xml` — an exception naming a file nobody configured, thrown from the log-level stamp rather than from the home. An embedder now gets a message naming the home, the `-Dde.griefed.serverpackcreator.home` that overrides it and the `Preferences` node the stored value lives in. The exception type changes (`IOException` → `IllegalStateException`, i.e. unchecked), so a `catch (IOException)` around construction no longer catches it; a host embedding SPC in a writable home sees no difference at all. The setter half goes the other way: writing a log level into an unreadable or unwritable `log4j2.xml` is now logged at ERROR instead of thrown, so a GUI or web caller that changes the level no longer has to guard the call — the write above it was already tolerant, and that asymmetry is what made an unwritable home fatal. Pinned by `ApiPropertiesHomeDirectoryTest`. |
| `PathsConfig` takes the process working directory as a defaulted constructor parameter, and uses it as a source build's home **only when it is writable** | Source-compatible — the parameter defaults to the `File("").absoluteFile` it replaces, so no caller changes. What changes for an embedder is which home a *source build with nothing configured* resolves: an unwritable working directory is skipped in favour of the next candidate, `<user.home>/ServerPackCreator`. A normal dev checkout is writable and resolves exactly as before. The case this fixes is a service: `systemd` starts a unit in `/`, and every write SPC performs then failed. Pinned by `PathsConfigTest.theDevEnvironmentFallbackSkipsAnUnwritableWorkingDirectory`. |
| `File.testFileWrite()` probes with a per-call file name (`Files.createTempFile(dir, ".spc-write-probe", null)`) instead of a fixed `poke` | Same signature, same contract — *false* on failure, `IllegalArgumentException` on a non-directory — and a **more truthful answer**. The old fixed name was defeated two ways: anything in the directory already holding it (a directory named `poke`, or a leftover from a probe whose delete failed) made a writable directory report unwritable, and two probes on one directory raced, the second's `exists()` check losing to the first's delete. Measured before the fix: **34 of 64** concurrent probes on a writable directory answered false. An embedder sees no API change, but two things that could previously fail now do not — and one thing it may have relied on is gone: the probe no longer creates a file called `poke`, so anything watching for that name sees nothing. It also no longer writes *content*, only creates and removes a file, which is the directory permission actually being tested. Pinned by `TestFileWriteTest`, whose deterministic guard plants something on the probe's old name and whose concurrency guard runs 64 probes at once. |
| `FabricScanner` and `QuiltScanner` stop excluding **Fabric API** (`fabric`) and **QFAPI** (`quilted_fabric_api`) from `ScannedMod.dependencies`; `ModDependency` gains `versionConstraint` behind `@JvmOverloads` | **Behaviour change on published API, and a bug fix.** Both exclusion sets carry the doc comment *"ids that are the platform rather than a mod"*, and both broke it: `fabricloader`, `quilt_loader` and `quilt_base` are the platform, while `fabric` is Fabric API — the most-depended-on **mod** in the ecosystem — and `quilted_fabric_api` is Quilt's port of it. Both are genuinely required on a server by the mods that declare them, so an embedder reading `dependencies` was told a Fabric mod depends on nothing but `creativecore`. What an embedder now sees: those ids appear in the list, and `ModListCompiler`'s dependency-rescue loop consequently pulls a disabled Fabric API / QFAPI jar **back into** a server pack when a kept mod depends on it — correct, since you cannot strip Fabric API from a Fabric server. Checked before landing: **neither id is in the shipped fallback clientside list**, so on a stock install nothing is disabled to be rescued and generation is byte-identical; the change becomes protective only where a custom or polled list contains such an entry. `String.matches` is a full match, so `fabric-api-base` was never affected either way. `versionConstraint` is the descriptor's range **verbatim and unparsed** (`>=0.92.0`, `[15.2,)`) because the grammars differ per loader; `@JvmOverloads` keeps the old `(String)V` and `(String, Sideness)V` JVM constructor descriptors, which is the contract that actually binds — pf4j loads *compiled* plugin jars, so binary compatibility, not merely source, is what a plugin depends on. `toString()` gains the new field. Pinned by `ModScannerSidenessTest` (`fabricApiIsADependencyAndCarriesItsVersionRange`, `quiltedFabricApiIsADependencyRatherThanThePlatform`, `forgeDependenciesCarryTheirDeclaredVersionRange`, `aBareQuiltDependencyCarriesNoConstraint`). |
| `ScannedMod` gains `provides: List<String>` behind `@JvmOverloads`; `FabricScanner` and `QuiltScanner` now read the descriptor's `provides` block, and `ModListCompiler`'s dependency rescue matches a dependency against a mod's id **and** its aliases | **Behaviour change on published API, completing a half-landed fix.** The earlier exclusion change made `fabric` a real dependency again, but the rescue could not use it: it matched `ModDependency.modID` against the disabled mod's own `modID` literally. Verified against the real artifact — Fabric API **0.92.11+1.20.1**, fetched from Modrinth's CDN, declares `"id": "fabric-api"` with `"provides": ["fabric"]` (53 nested jars) — so a mod writing `depends: {"fabric": "*"}` names an id no jar in the pack calls itself, `"fabric"` was compared to `"fabric-api"`, and a custom clientside list naming Fabric API stripped it out from under that mod. The resulting pack installs and dies on load, which is precisely what the rescue exists to prevent. What an embedder now sees: `ScannedMod.provides` carries the aliases (empty for Forge and NeoForge, which have no such concept, and empty for the newest Fabric API builds, which have dropped the block — so its absence stays normal rather than an error); a disabled mod is rescued when a kept server mod depends on any id it answers to. `@JvmOverloads` preserves the previous `(File, String, Sideness, List)` JVM constructor descriptor, which is the contract that binds — pf4j loads *compiled* plugin jars. Found by running B0's generation regression check, which had been specified and never run. Pinned by `ModScannerSidenessTest` (`fabricProvidesIsRecordedAsAnAlias`, `aFabricModWithoutProvidesCarriesNoAliases`, `quiltProvidesIsReadInBothOfItsEntryShapes`) and `ModListCompilerTest` (`aHistoricalFabricDependencyAlsoRescuesFabricApi`, `aFabricApiDependencyIsRescuedFromAClientsideList`, `aStockPackContainingFabricApiIsUnchanged`). |
| `ScannedMod.minecraftConstraint` — the Minecraft version range a descriptor declares, verbatim | **Additive; nothing existing changes.** Every scanner already parsed this value and discarded it: Fabric and Quilt exclude `minecraft` from `dependencies` as "the platform", and the Forge/NeoForge tomls *consume* the platform entry for its `side`, which is what decides the mod's own sideness. So an embedder gains an answer it could not previously get — **what did this jar say it was built for?** — while `dependencies`, `provides` and `sideness` are byte-for-byte unchanged (`theMinecraftEntryIsStillNotADependency` and `forgeReportsTheMinecraftRangeWithoutLosingItsSideness` pin both halves; the Forge reader is deliberately separate from `getSidenessesAndDependencies` so capturing the range cannot disturb the inference that consumes the same entry). `null` when the descriptor states none, never an invented range, and the text is kept **unparsed** because the grammars differ per loader — Fabric and Quilt write npm-style ranges (`~1.20.1`), Forge and NeoForge Maven ranges (`[1.19.2,1.20)`) — so normalising here would lose the information a consumer needs to tell them apart. The constructor parameter is trailing and defaulted, and `ScannedMod` already carries `@JvmOverloads`, so the old JVM descriptors survive for pre-compiled pf4j plugins. **Why it was added:** the grinder booted the *newest* Minecraft a platform declared for a file without ever asking what the jar targeted, which put real mods on the wrong mappings and scored the resulting mixin failures as clientside crashes. |
| `QuiltPackScanner` returns the **Fabric** scan for a jar carrying no `quilt.mod.json`; `ScannedMod` gains `descriptorRead` | **Behaviour change on published API, and a bug fix with a user-facing half.** The merge previously returned the Quilt scan and preferred the Fabric one only where the two disagreed about *sideness* (`quilt == SERVER && fabric == CLIENT`). Quilt deliberately runs Fabric mods and most ship no Quilt descriptor at all, so the Quilt scan yielded `DescriptorScanner`'s "nothing could be read" fallback — file name as `modID`, `SERVER`, empty `dependencies`, empty `provides`, `null` `minecraftConstraint` — the Fabric scan held the real descriptor, **both read `SERVER`, they agreed, and the empty entry won.** Everything the jar declared was discarded. What an embedder now sees for such a jar: the declared id instead of the file name, and the dependency/`provides`/Minecraft data that was previously absent. **Why it matters beyond a scan result:** `ModListCompiler`'s dependency rescue reads this, so a Quilt pack could have Fabric API stripped while a kept mod depended on it — the same outcome as the `fabric` exclusion bug, reached by a different route. Found from a live grinder boot: `bookshelf` on Quilt 0.31.0-beta.3 / Minecraft 1.21.1 died with `Bookshelf requires any version of fabric-api, which is missing!`, because the dependency was never *reported*, so nothing downstream could resolve it. `descriptorRead` is additive and defaults to `false` — the fallback's value — so only a scanner that genuinely parsed a descriptor reports `true`; it exists because the fallback is otherwise **indistinguishable by value** from a real scan of a mod that declares nothing. Pinned by `ModScannerSidenessTest.aFabricOnlyJarKeepsItsDependenciesWhenScannedForQuilt`, whose red state read `expected: <bookshelf> but was: <bookshelf-fabric>`. |
| `QuiltScanner.dependencyExclusions` no longer excludes `quilt_base`, so a Quilt mod declaring it now reports it in `ScannedMod.dependencies` | Every signature is unchanged; what changes is the *contents* of the list an embedder reads. A Quilt descriptor's `quilt_base` entry was silently dropped as though it were the runtime, and is now recorded like any other mod dependency — so a scan of the same jar returns one more `ModDependency` than it did before. `quilt_base` is QSL's base module shipped by QFAPI (`library/core/qsl_base` in `QuiltMC/quilt-standard-libraries`; its `quilt_base_testmod` depends on `["quilt_loader", "quilt_base"]`), not the platform — the same distinction `FabricScanner` already draws by excluding `fabricloader` but never `fabric`. **Consequence for generation:** dependency rescue keeps a jar that something depends on, so a QFAPI/QSL jar can now be rescued into a server pack that had disabled it, where before nothing could name it as a dependency. That is the intended fix, but an embedder asserting on a fixed dependency count for a Quilt jar will see the count rise by one. `quilt_loader` remains excluded. |
| `ModDependency.optional` — new property, and `ForgeTomlScanner`/`NeoForgeTomlScanner` now populate it from Forge's `mandatory` and NeoForge's `type` | **Additive; nothing an embedder already reads changes value.** `ScannedMod.dependencies` still lists every declared dependency, in the same order — an optional one is flagged, not removed, so `ModListCompiler`'s dependency rescue and any embedder counting or matching dependencies behave exactly as before. What is new is that an embedder *can* now tell a `mandatory=false` / `type="optional"` dependency from a required one, which was previously impossible: neither word was read anywhere in the module. `optional` defaults to `false`, so a hand-constructed `ModDependency` keeps its old meaning and every positional constructor call still compiles. Consumers that treated the list as "all required" are still correct-by-default but are now wrong in principle — `-clientside`'s boot staging is the first to filter on it. |
| **Version-metadata accessors return immutable snapshots, and three signatures narrowed** — `LegacyFabricMeta.supportedMinecraftVersions()` `MutableList<String>` → `List<String>`, `ForgeMeta.getForgeMeta()` and `NeoForgeMeta.getNeoForgeMeta()` `HashMap<String, List<String>>` → `Map<String, List<String>>` | **Source-breaking for a caller that declared the old type or mutated the result**, which is why the narrowing was made rather than avoided: those types advertised that a plugin could mutate ServerPackCreator's own version metadata, and `VersionMeta` refreshes that metadata on a background coroutine. Every accessor across `versionmeta` now hands out an unmodifiable snapshot, so a held reference no longer changes underneath the holder and can no longer throw `ConcurrentModificationException` mid-iteration — the defect this fixes, reproduced with the refresh coroutine in its own stack trace. A caller that only reads is unaffected; one that mutated was corrupting shared state. |
| `ApiPlugins.getAllExtensionsOfPlugin(plugin, type)` scopes its answer to `plugin` (`pluginManager.getExtensions(type, plugin.pluginId)`) instead of returning every plugin's extensions | **Behaviour change on published API, and a bug fix that only becomes visible with two plugins installed.** The `plugin` argument was accepted and ignored, so the method answered with every extension of that type the manager knew. Its callers all loop over the installed plugins and call it once each, which multiplied the result: `addTabExtensionTabs` added each tab once per *installed plugin*, and `runPreGenExtensions` / `runPreZipExtensions` / `runPostGenExtensions` / `runConfigCheckExtensions` ran each extension that many times. With a single plugin — all this repository shipped until the grinder plugin — one times one is one and nothing looked wrong; reproduced 2026-09-06 with two plugins installed, where the tab strip read `Grinder \| Tetris \| Grinder \| Tetris`. What an embedder sees: with one plugin, nothing changes. With N plugins, each tab now appears once instead of N times, and each generation extension runs once instead of N times — so a plugin whose `PostGenExtension` was (unknowingly) running N times, or one relying on seeing another plugin's extensions through this call, changes behaviour. A `ConfigCheckExtension` returning "error" no longer does so N times. Pinned by `ExtensionScopingTest`, which builds its second plugin by cloning the example jar under a new id, since the guard is meaningless with fewer than two. |
